name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: yarn

      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Lint
        run: yarn lint

      - name: Check types
        run: yarn workspace react-motion-modal check-types

      - name: Build library
        run: yarn workspace react-motion-modal build

      - name: Run tests
        working-directory: apps/library
        run: yarn test

      - name: Verify publish contents
        working-directory: apps/library
        run: npm pack --dry-run
